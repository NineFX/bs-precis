let identifierClass:
  (list(BsPrecis.PrecisUtils.codePoint), bool) =>
  list(BsPrecis.PrecisUtils.codePoint);
let identifierEnforce:
  list(BsPrecis.PrecisUtils.codePoint) =>
  list(BsPrecis.PrecisUtils.codePoint);
let freeformClass:
  (list(BsPrecis.PrecisUtils.codePoint), bool) =>
  list(BsPrecis.PrecisUtils.codePoint);
let freeformEnforce:
  list(BsPrecis.PrecisUtils.codePoint) =>
  list(BsPrecis.PrecisUtils.codePoint);
